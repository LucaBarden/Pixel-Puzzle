services:
  pixel-puzzle:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./images:/app/images
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
